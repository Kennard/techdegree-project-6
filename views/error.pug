extends layout.pug

block content
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back
  article.grid-container.portfolio-intro.portfolio-project
    .grid-x
      .cell.small-6.medium-9.large-9
        //- Adding errors to the screen error object
        h3= error.message
        h1= error.status
          h2 Dangit!  
            p= error.stack
  nav.grid-container.portfolio-breadcrumb
    a(href='/') ← Back